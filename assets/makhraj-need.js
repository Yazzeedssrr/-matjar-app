(()=>{'use strict';
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const norm=s=>String(s||'').toLowerCase().normalize('NFKD').replace(/[ًٌٍَُِّْـ]/g,'').trim();
const intentGroups={
 gift:['هدية','هديه','gift','زوجتي','زوجي','عيد','مناسبة','مناسبه','تخرج','birthday'],
 car:['سيارة','سياره','car','سيارتي','قيادة','قياده','طريق'],
 work:['عمل','شغل','دوام','work','مكتب','توصيل'],
 travel:['سفر','رحلة','رحله','travel','طائرة','طائره'],
 urgent:['اليوم','الليلة','الليله','الآن','الان','سريع','عاجل'],
 budget:['رخيص','اقتصادي','ميزانية','ميزانيه','cheap'],
 premium:['فاخر','فخم','مميز','premium']
};
function tokens(q){const n=norm(q);const out=new Set(n.split(/\s+/).filter(x=>x.length>1));Object.entries(intentGroups).forEach(([k,arr])=>{if(arr.some(x=>n.includes(norm(x))))out.add(k)});return [...out]}
function productText(p){return norm([p.name,p.description,p.brand,(p.tags||[]).join(' '),(p.need_tags||[]).join(' '),(p.situations||[]).join(' '),p.benefit_summary,p.ideal_for,Object.entries(p.specifications||{}).flat().join(' ')].join(' '))}
function score(p,ts){const text=productText(p);let s=0,reasons=[];ts.forEach(t=>{if(text.includes(t)){s+=3;reasons.push(t)};(p.need_tags||[]).forEach(x=>{if(norm(x).includes(t)||t.includes(norm(x))){s+=5;reasons.push(x)}});(p.situations||[]).forEach(x=>{if(norm(x).includes(t)||t.includes(norm(x))){s+=4;reasons.push(x)}})});if(p.featured)s+=1;const stock=(p.product_variants||[]).reduce((a,v)=>a+Number(v.stock_quantity||0),0);if(stock>0)s+=1;else s-=50;return {score:s,reasons:[...new Set(reasons)].slice(0,3)}}
async function fetchProducts(){try{const cfg=window.MAKHRAJ_CONFIG,sb=window.supabase.createClient(cfg.supabaseUrl,cfg.supabaseKey);const {data,error}=await sb.from('products').select('id,name,description,brand,tags,need_tags,situations,benefit_summary,ideal_for,base_price,featured,specifications,product_images(url,sort_order),product_variants(id,stock_quantity,price)').eq('status','active');if(error)throw error;return data||[]}catch(e){console.error(e);return[]}}
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:(window.MAKHRAJ_CONFIG?.currency||'USD')}).format(Number(n||0));
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function solve(q){q=q.trim();if(!q)return;const section=$('#needResults'),grid=$('#needProductGrid'),reason=$('#needReason');section.classList.remove('hidden');$('#needTitle').textContent='مخارج لـ «'+q+'»';reason.textContent='نحلل حاجتك ونرتب المنتجات بحسب صلتها بما طلبته، لا بحسب إعلان مدفوع.';grid.innerHTML='<div class="loading">جارٍ البحث عن أقرب مخرج…</div>';section.scrollIntoView({behavior:'smooth',block:'start'});const ps=await fetchProducts(),ts=tokens(q);let ranked=ps.map(p=>({...score(p,ts),p})).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).slice(0,6);if(!ranked.length)ranked=ps.filter(p=>(p.product_variants||[]).some(v=>v.stock_quantity>0)).slice(0,4).map(p=>({p,score:0,reasons:[]}));if(!ranked.length){grid.innerHTML='<div class="empty"><b>لا يوجد مخرج تجاري لهذه الحاجة بعد.</b><br><span class="tiny">هذه نتيجة صادقة: لم نضف منتجات مناسبة بعد، ولن نخترع توصية.</span></div>';return}grid.innerHTML=ranked.map((x,i)=>{const p=x.p,img=[...(p.product_images||[])].sort((a,b)=>a.sort_order-b.sort_order)[0]?.url,prices=(p.product_variants||[]).filter(v=>v.stock_quantity>0).map(v=>Number(v.price??p.base_price)),price=prices.length?Math.min(...prices):p.base_price;return '<article class="card need-card"><div class="need-rank">'+(i===0?'الأقرب لحاجتك':'خيار '+(i+1))+'</div><div class="pimg" '+(img?'style="background-image:url(\''+esc(img)+'\')"':'')+'></div><div class="pbody"><h3>'+esc(p.name)+'</h3>'+(p.benefit_summary?'<p class="muted need-why">'+esc(p.benefit_summary)+'</p>':'')+(x.reasons.length?'<div class="tiny">لأنه يرتبط بـ: '+esc(x.reasons.join(' · '))+'</div>':'')+'<div class="line" style="margin-top:10px"><span class="price">'+money(price)+'</span><button class="primary need-open" data-id="'+p.id+'">شاهده</button></div></div></article>'}).join('');$$('.need-open',grid).forEach(b=>b.onclick=()=>{const card=document.querySelector('[data-open="'+CSS.escape(b.dataset.id)+'"]');if(card){card.click()}else{document.querySelector('#searchInput').value=ranked.find(x=>x.p.id===b.dataset.id)?.p.name||'';document.querySelector('#searchInput').dispatchEvent(new Event('input'));document.querySelector('#productGrid').scrollIntoView({behavior:'smooth'})}})}
function init(){const input=$('#needInput'),btn=$('#needSolveBtn');if(!input||!btn)return;btn.onclick=()=>solve(input.value);input.addEventListener('keydown',e=>{if(e.key==='Enter')solve(input.value)});$$('[data-need]').forEach(b=>b.onclick=()=>{input.value=b.dataset.need;solve(input.value)});$('#clearNeedBtn').onclick=()=>{$('#needResults').classList.add('hidden');input.value=''};}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();